/* ============================================================
   CONFIG — API base URL
   ============================================================ */

const CONFIG = {
  // For local dev: empty string = same origin (localhost:8000 proxy handles /api/*).
  // For production (GitHub Pages): set to the full Heroku URL.
  API_URL: "https://dynasty-im-backend-ccc68ed32453.herokuapp.com",
};
