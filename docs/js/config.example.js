/* ============================================================
   CONFIG — Copy this to config.js and fill in real values.
   config.js is gitignored and will NOT be committed.
   ============================================================ */

const CONFIG = {
  API_URL: 'https://your-heroku-app.herokuapp.com',
  FRONTEND_TOKEN: 'your-frontend-token-here'
};
